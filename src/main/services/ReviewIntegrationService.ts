import { randomUUID } from 'node:crypto';
import { spawn } from 'node:child_process';
import { lstat, mkdir, readFile, readdir, rmdir } from 'node:fs/promises';
import { join, resolve } from 'node:path';
import type { Run, RunIntegrationReview, ReviewerVerdict } from '@shared/domain/entities';
import type { RunRepository } from '../persistence/repositories/RunRepository';
import type { WorkspaceRepository } from '../persistence/repositories/WorkspaceRepository';
import type { SettingsRepository } from '../persistence/repositories/SettingsRepository';
import type { RunIntegrationReviewRepository } from '../persistence/repositories/RunIntegrationReviewRepository';
import type { AgentAdapter } from './contracts/AgentAdapter';
import type { ProcessSupervisor } from './contracts/ProcessSupervisor';
import type { RunArtifactCleaner } from './contracts/RunArtifactCleaner';
import { runGit, type GitCommand } from './GitWorktreeService';
import type { RunReviewService } from './RunReviewService';
import { WindowsProcessSupervisor } from './WindowsProcessSupervisor';

export interface ReviewerRunner { review(input: { runId: string; workspaceId: string; candidateSha: string; targetDevSha: string; worktreePath: string; agentId: string; modelId: string; prompt: string; evidence: unknown }): Promise<string>; }
export interface IntegrationValidator { validate(path: string): Promise<{ passed: boolean; evidence: string }>; }
export type CandidateProgressionMode = 'candidate_only' | 'auto_review' | 'auto_review_integrate';
export const devBaseKey = (workspaceId: string): string => `nightshift.workspace.${workspaceId}.canonical_dev_sha`;
export const candidateProgressionKey = (workspaceId: string): string => `nightshift.workspace.${workspaceId}.candidate_progression_mode`;
export const resolveEffectiveDevBase = async (rootPath: string, workspaceId: string, settings: SettingsRepository | undefined, git: GitCommand = runGit): Promise<string> => {
  const preferred = settings?.get<string>(devBaseKey(workspaceId)) ?? 'refs/heads/dev'; const result = await git(rootPath, ['rev-parse', '--verify', `${preferred}^{commit}`]);
  if (result.exitCode !== 0) throw new Error('NightShift effective dev base is unavailable locally.'); return result.stdout.trim();
};
export class AdapterReviewerRunner implements ReviewerRunner { public constructor(private readonly adapters: ReadonlyMap<string, AgentAdapter>, private readonly timeoutMs = 10 * 60_000) {} public async review(input: Parameters<ReviewerRunner['review']>[0]): Promise<string> { const adapter = this.adapters.get(input.agentId); if (!adapter) throw new Error(`Reviewer agent ${input.agentId} is unavailable.`); const handle = await adapter.startRun({ runId: `review-${input.runId}-${randomUUID()}`, workspaceId: input.workspaceId, workingDirectory: input.worktreePath, modelId: input.modelId, prompt: input.prompt }); let timer: ReturnType<typeof setTimeout> | undefined; try { const result = await Promise.race([handle.completion, new Promise<never>((_resolve, reject) => { timer = setTimeout(() => reject(new Error('Reviewer exceeded the hard timeout.')), this.timeoutMs); })]); if (!result.succeeded) throw new Error(result.failureReason ?? 'Reviewer failed.'); return result.terminalEvent?.raw ?? ''; } catch (error) { await adapter.cancel(handle.handleId).catch(() => undefined); throw error; } finally { if (timer) clearTimeout(timer); } } }

export class ReviewIntegrationArtifactCleaner implements RunArtifactCleaner {
  public constructor(private readonly reviews: RunIntegrationReviewRepository, private readonly workspaces: WorkspaceRepository, private readonly storageRoot: string, private readonly git: GitCommand = runGit) {}
  public async removeForRuns(runs: readonly Run[]): Promise<void> {
    const reviews = this.reviews.listByRunIds(runs.map((run) => run.id)); const byRun = new Map<string, string[]>();
    for (const review of reviews) byRun.set(review.runId, [...(byRun.get(review.runId) ?? []), review.id]);
    const prunedRoots = new Set<string>();
    for (const run of runs) { const workspace = this.workspaces.findById(run.workspaceId); if (!workspace?.isGit) continue; if (!prunedRoots.has(workspace.rootPath)) { const pruned = await this.git(workspace.rootPath, ['worktree', 'prune', '--expire=now']); if (pruned.exitCode !== 0) throw new Error(`Could not prune stale review worktree metadata: ${detail(pruned)}`); prunedRoots.add(workspace.rootPath); } await this.removeReviewerWorktrees(workspace.rootPath, run.id); for (const reviewId of byRun.get(run.id) ?? []) await this.removeWorktree(workspace.rootPath, join(this.storageRoot, 'integrations', reviewId)); }
  }
  public async removeWorktree(repositoryRoot: string, path: string): Promise<void> {
    try { await lstat(path); } catch (error) { if (isMissing(error)) { const pruned = await this.git(repositoryRoot, ['worktree', 'prune', '--expire=now']); if (pruned.exitCode !== 0) throw new Error(`Could not prune stale review worktree metadata: ${detail(pruned)}`, { cause: error }); return; } throw error; }
    const root = await this.git(path, ['rev-parse', '--show-toplevel']); if (root.exitCode !== 0) throw new Error(`Refusing to remove an unrecognized review worktree: ${path}`);
    const [worktreeCommon, repositoryCommon] = await Promise.all([this.git(path, ['rev-parse', '--git-common-dir']), this.git(repositoryRoot, ['rev-parse', '--git-common-dir'])]);
    if (worktreeCommon.exitCode !== 0 || repositoryCommon.exitCode !== 0 || resolve(root.stdout.trim()) !== resolve(path) || resolve(path, worktreeCommon.stdout.trim()) !== resolve(repositoryRoot, repositoryCommon.stdout.trim())) throw new Error(`Refusing to remove an unrecognized review worktree: ${path}`);
    const removed = await this.git(repositoryRoot, ['worktree', 'remove', '--force', path]); if (removed.exitCode !== 0) throw new Error(`Could not remove review worktree: ${detail(removed)}`);
  }
  private async removeReviewerWorktrees(repositoryRoot: string, runId: string): Promise<void> {
    const directory = join(this.storageRoot, 'reviews', runId); let entries;
    try { entries = await readdir(directory, { withFileTypes: true }); } catch (error) { if (isMissing(error)) return; throw error; }
    for (const entry of entries) if (entry.isDirectory()) await this.removeWorktree(repositoryRoot, join(directory, entry.name));
    await rmdir(directory).catch((error: unknown) => { if (!isMissing(error) && (error as NodeJS.ErrnoException).code !== 'ENOTEMPTY') throw error; });
  }
}

export class ReviewIntegrationService {
  private readonly artifacts: ReviewIntegrationArtifactCleaner;
  private readonly automaticQueues = new Map<string, { operation: Promise<void>; dirty: boolean }>();
  public constructor(private readonly runs: RunRepository, private readonly workspaces: WorkspaceRepository, private readonly reviews: RunIntegrationReviewRepository, private readonly reviewer: ReviewerRunner, private readonly reviewReader: RunReviewService, private readonly storageRoot: string, private readonly settings?: SettingsRepository, private readonly validator: IntegrationValidator = new SupervisedIntegrationValidator(), private readonly git: GitCommand = runGit) { this.artifacts = new ReviewIntegrationArtifactCleaner(reviews, workspaces, storageRoot, git); }
  public latest(runId: string): RunIntegrationReview | undefined { return this.reviews.latest(runId); }
  public getCandidateProgressionMode(workspaceId: string): CandidateProgressionMode { const value = this.settings?.get<CandidateProgressionMode>(candidateProgressionKey(workspaceId)); return value === 'auto_review' || value === 'auto_review_integrate' ? value : 'candidate_only'; }
  public setCandidateProgressionMode(workspaceId: string, mode: CandidateProgressionMode): CandidateProgressionMode { if (!this.workspaces.findById(workspaceId)) throw new Error('Workspace is unavailable.'); if (!['candidate_only', 'auto_review', 'auto_review_integrate'].includes(mode)) throw new Error('Unknown candidate progression mode.'); this.settings?.set(candidateProgressionKey(workspaceId), mode); if (mode !== 'candidate_only') void this.enqueueAutomatic(workspaceId); return mode; }
  public onCandidatePublished(run: Run): void { if (this.getCandidateProgressionMode(run.workspaceId) !== 'candidate_only') void this.enqueueAutomatic(run.workspaceId); }
  public async resumeAutomaticWork(): Promise<void> { await Promise.all(this.workspaces.list().filter((workspace) => this.getCandidateProgressionMode(workspace.id) !== 'candidate_only').map((workspace) => this.enqueueAutomatic(workspace.id))); }
  private enqueueAutomatic(workspaceId: string): Promise<void> {
    const active = this.automaticQueues.get(workspaceId);
    if (active) { active.dirty = true; return active.operation; }
    const state: { operation: Promise<void>; dirty: boolean } = { operation: Promise.resolve(), dirty: false };
    state.operation = (async () => {
      do { state.dirty = false; await this.drainAutomatic(workspaceId); } while (state.dirty);
    })().catch((error: unknown) => console.error(`[Integration queue] ${workspaceId} stopped.`, error)).finally(() => this.automaticQueues.delete(workspaceId));
    this.automaticQueues.set(workspaceId, state);
    return state.operation;
  }
  private async drainAutomatic(workspaceId: string): Promise<void> {
    const mode = this.getCandidateProgressionMode(workspaceId); if (mode === 'candidate_only') return;
    for (const run of this.runs.publishedValidated(workspaceId)) {
      const latest = this.reviews.latest(run.id);
      if (mode === 'auto_review') { if (!latest) await this.requestReview(run.id); continue; }
      if (latest?.integrationStatus === 'integrated' || latest?.integrationStatus === 'needs_attention' || latest?.integrationStatus === 'rejected') continue;
      await this.automaticIntegrate(run, latest);
    }
  }
  private async automaticIntegrate(run: Run, recovered: RunIntegrationReview | undefined): Promise<void> {
    const workspace = this.workspaces.findById(run.workspaceId); if (!workspace?.isGit || !run.candidateCommitSha) return;
    const remote = run.candidateRemoteName ?? 'origin';
    if (recovered?.integrationStatus === 'integrating' && recovered.integrationCommitSha && await this.remote(workspace.rootPath, remote, 'dev') === recovered.integrationCommitSha) { this.settings?.set(devBaseKey(run.workspaceId), recovered.integrationCommitSha); this.reviews.setIntegration(recovered.id, 'integrated', { commitSha: recovered.integrationCommitSha, validation: recovered.integrationValidation }); return; }
    if (recovered?.integrationStatus === 'integrating') this.reviews.setIntegration(recovered.id, 'needs_attention', { validation: recovered.integrationValidation, failureReason: 'Automatic integration was interrupted before push; rebuilt safely.' });
    const base = await this.remote(workspace.rootPath, remote, 'dev');
    if (!base) return;
    await this.must(workspace.rootPath, ['fetch', remote, 'refs/heads/dev'], 'Could not fetch current remote dev.');
    const candidate = await this.commit(workspace.rootPath, run.candidateCommitSha);
    if (!run.candidateBranchName || await this.remote(workspace.rootPath, remote, run.candidateBranchName) !== candidate) { this.createAutomaticFailure(run, candidate, base, 'Remote candidate no longer matches the published candidate SHA.'); return; }
    const path = join(this.storageRoot, 'integrations', `auto-${randomUUID()}`); await mkdir(join(this.storageRoot, 'integrations'), { recursive: true });
    let review: RunIntegrationReview | undefined;
    try {
      await this.must(workspace.rootPath, ['worktree', 'add', '--detach', path, base], 'Could not create automatic integration worktree.');
      const merged = await this.git(path, ['merge', '--no-ff', '--no-edit', candidate]);
      if (merged.exitCode !== 0) { this.createAutomaticFailure(run, candidate, base, `Merge conflict or failure: ${detail(merged)}`); return; }
      const validation = await this.validator.validate(path);
      if (!validation.passed) { const failed = this.createAutomaticFailure(run, candidate, base, 'Integrated-tree validation failed or timed out.'); this.reviews.setIntegration(failed.id, 'needs_attention', { validation: validation.evidence, failureReason: 'Integrated-tree validation failed or timed out.' }); return; }
      const mergedSha = await this.commit(path, 'HEAD');
      review = this.reviews.create({ runId: run.id, candidateSha: candidate, targetDevSha: base, reviewerAgentId: run.resolvedAgentId, reviewerModelId: run.resolvedModelId, verdict: 'NEEDS_ATTENTION', summary: 'Automatic merged-result review pending.', findings: '' });
      this.reviews.setIntegration(review.id, 'integrating', { commitSha: mergedSha, validation: validation.evidence });
      const verdict = await this.reviewMerged(run, path, mergedSha, base, validation.evidence);
      review = this.reviews.setVerdict(review.id, verdict.verdict, verdict.summary, verdict.findings);
      if (review.verdict !== 'PASS') { this.reviews.setIntegration(review.id, 'needs_attention', { commitSha: mergedSha, validation: validation.evidence, failureReason: `Reviewer verdict: ${review.verdict}.` }); return; }
      if (await this.remote(workspace.rootPath, remote, 'dev') !== base) { this.reviews.markStale(review.id, 'Remote dev moved before automatic push.'); return; }
      const pushed = await this.git(path, ['push', remote, 'HEAD:refs/heads/dev']);
      if (pushed.exitCode !== 0) { this.reviews.setIntegration(review.id, 'needs_attention', { commitSha: mergedSha, validation: validation.evidence, failureReason: `Push rejected: ${detail(pushed)}` }); return; }
      this.settings?.set(devBaseKey(run.workspaceId), mergedSha); this.reviews.setIntegration(review.id, 'integrated', { commitSha: mergedSha, validation: validation.evidence });
    } catch (error) { if (review) this.reviews.setIntegration(review.id, 'needs_attention', { failureReason: error instanceof Error ? error.message : String(error) }); else this.createAutomaticFailure(run, candidate, base, error instanceof Error ? error.message : String(error)); }
    finally { await this.artifacts.removeWorktree(workspace.rootPath, path).catch((reason: unknown) => console.error('[Integration] Could not clean automatic worktree.', reason)); }
  }
  private createAutomaticFailure(run: Run, candidate: string, base: string, reason: string): RunIntegrationReview { const review = this.reviews.create({ runId: run.id, candidateSha: candidate, targetDevSha: base, reviewerAgentId: run.resolvedAgentId, reviewerModelId: run.resolvedModelId, verdict: 'NEEDS_ATTENTION', summary: reason, findings: reason }); return this.reviews.setIntegration(review.id, 'needs_attention', { failureReason: reason }); }
  private async reviewMerged(run: Run, path: string, mergedSha: string, base: string, validation: string): Promise<{ verdict: ReviewerVerdict; summary: string; findings: string }> { const compared = await boundedGit(path, ['diff', '--no-ext-diff', '--binary', '--find-renames', base, mergedSha], REVIEW_PATCH_BYTES); const evidence = { integrationValidation: validation, mergedChangedFiles: (await this.git(path, ['diff', '--name-status', '--find-renames', base, mergedSha])).stdout, mergedPatch: compared.stdout, patchTruncated: compared.truncated }; const before = await this.state(path); let raw = ''; let error: string | null = null; try { raw = await this.reviewer.review({ runId: run.id, workspaceId: run.workspaceId, candidateSha: mergedSha, targetDevSha: base, worktreePath: path, agentId: run.resolvedAgentId, modelId: run.resolvedModelId, prompt: prompt(mergedSha, base, evidence), evidence }); } catch (reason) { error = reason instanceof Error ? reason.message : String(reason); } const after = await this.state(path); const parsed = parse(raw); const unsafe = before.head !== after.head || before.status !== after.status || await this.commit(path, 'HEAD') !== mergedSha; return unsafe || error || !parsed ? { verdict: 'NEEDS_ATTENTION', summary: unsafe ? 'Reviewer or repository mutation detected.' : error ?? 'Malformed reviewer verdict contract.', findings: unsafe ? `before=${JSON.stringify(before)} after=${JSON.stringify(after)}` : raw.slice(0, 16 * 1024) } : parsed; }
  public async requestReview(runId: string): Promise<RunIntegrationReview> {
    const run = this.runs.findRequired(runId); this.gate(run); const workspace = this.workspaces.findById(run.workspaceId); if (!workspace?.isGit) throw new Error('Review requires a Git workspace.'); const candidateSha = await this.commit(workspace.rootPath, run.candidateCommitSha!); const targetDevSha = await resolveEffectiveDevBase(workspace.rootPath, run.workspaceId, this.settings, this.git); const runEvidence = await this.reviewReader.automationEvidence(runId); const compared = await boundedGit(workspace.rootPath, ['diff', '--no-ext-diff', '--binary', '--find-renames', targetDevSha, candidateSha], REVIEW_PATCH_BYTES); if (compared.exitCode !== 0) throw new Error('Could not build candidate-versus-dev review patch.'); const evidence = { review: runEvidence.review, candidateChangedFiles: (await this.git(workspace.rootPath, ['diff', '--name-status', '--find-renames', targetDevSha, candidateSha])).stdout, candidatePatch: compared.stdout, patchTruncated: compared.truncated }; const path = join(this.storageRoot, 'reviews', runId, randomUUID()); await mkdir(join(this.storageRoot, 'reviews', runId), { recursive: true }); await this.must(workspace.rootPath, ['worktree', 'add', '--detach', path, candidateSha], 'Could not create reviewer worktree.');
    try { const before = await this.state(path); let raw = ''; let error: string | null = null; try { raw = await this.reviewer.review({ runId, workspaceId: run.workspaceId, candidateSha, targetDevSha, worktreePath: path, agentId: run.resolvedAgentId, modelId: run.resolvedModelId, prompt: prompt(candidateSha, targetDevSha, evidence), evidence }); } catch (reason) { error = reason instanceof Error ? reason.message : String(reason); } const after = await this.state(path); const parsed = parse(raw); const unsafe = before.head !== after.head || before.status !== after.status || await this.commit(workspace.rootPath, candidateSha) !== candidateSha || await resolveEffectiveDevBase(workspace.rootPath, run.workspaceId, this.settings, this.git) !== targetDevSha; return this.reviews.create({ runId, candidateSha, targetDevSha, reviewerAgentId: run.resolvedAgentId, reviewerModelId: run.resolvedModelId, verdict: unsafe || error || !parsed ? 'NEEDS_ATTENTION' : parsed.verdict, summary: unsafe ? 'Reviewer or repository mutation detected.' : error ?? parsed?.summary ?? 'Malformed reviewer verdict contract.', findings: unsafe ? `before=${JSON.stringify(before)} after=${JSON.stringify(after)}` : parsed?.findings ?? raw.slice(0, 16 * 1024) }); }
    finally { await this.artifacts.removeWorktree(workspace.rootPath, path).catch((reason: unknown) => console.error('[Review] Could not clean reviewer worktree.', reason)); }
  }
  public async integrate(id: string): Promise<RunIntegrationReview> {
    const review = this.reviews.findRequired(id); if (review.integrationStatus === 'integrated') return review; if (review.verdict !== 'PASS') return this.reviews.setIntegration(id, 'rejected', { failureReason: 'Only PASS reviews may integrate.' }); if (review.staleAt) return review; const run = this.runs.findRequired(review.runId); const workspace = this.workspaces.findById(run.workspaceId); if (!workspace?.isGit) return this.reviews.setIntegration(id, 'rejected', { failureReason: 'Git workspace is unavailable.' }); const remote = run.candidateRemoteName ?? 'origin'; if (review.integrationStatus === 'integrating' && review.integrationCommitSha && await this.remote(workspace.rootPath, remote, 'dev') === review.integrationCommitSha) { this.settings?.set(devBaseKey(run.workspaceId), review.integrationCommitSha); return this.reviews.setIntegration(id, 'integrated', { commitSha: review.integrationCommitSha, validation: review.integrationValidation }); } const candidate = await this.commit(workspace.rootPath, run.candidateCommitSha ?? ''); const dev = await resolveEffectiveDevBase(workspace.rootPath, run.workspaceId, this.settings, this.git); const remoteCandidate = run.candidateBranchName ? await this.remote(workspace.rootPath, remote, run.candidateBranchName) : null; if (candidate !== review.candidateSha || dev !== review.targetDevSha || remoteCandidate !== review.candidateSha || await this.remote(workspace.rootPath, remote, 'dev') !== review.targetDevSha) return this.reviews.markStale(id, 'Candidate or dev no longer matches the reviewed SHA.'); this.reviews.setIntegration(id, 'integrating'); const path = join(this.storageRoot, 'integrations', id); await mkdir(join(this.storageRoot, 'integrations'), { recursive: true }); try { await this.must(workspace.rootPath, ['worktree', 'add', '--detach', path, review.targetDevSha], 'Could not create integration worktree.'); const merged = await this.git(path, ['merge', '--no-ff', '--no-edit', review.candidateSha]); if (merged.exitCode !== 0) return this.reviews.setIntegration(id, 'needs_attention', { failureReason: `Merge conflict or failure: ${detail(merged)}` }); const validation = await this.validator.validate(path); if (!validation.passed) return this.reviews.setIntegration(id, 'needs_attention', { validation: validation.evidence, failureReason: 'Integrated-tree validation failed or timed out.' }); if (await this.remote(workspace.rootPath, remote, 'dev') !== review.targetDevSha) return this.reviews.markStale(id, 'Remote dev moved before push.'); const sha = await this.commit(path, 'HEAD'); this.reviews.setIntegration(id, 'integrating', { commitSha: sha, validation: validation.evidence }); const pushed = await this.git(path, ['push', remote, 'HEAD:refs/heads/dev']); if (pushed.exitCode !== 0) return this.reviews.setIntegration(id, 'needs_attention', { validation: validation.evidence, failureReason: `Push rejected: ${detail(pushed)}` }); this.settings?.set(devBaseKey(run.workspaceId), sha); return this.reviews.setIntegration(id, 'integrated', { commitSha: sha, validation: validation.evidence }); } catch (error) { return this.reviews.setIntegration(id, 'needs_attention', { failureReason: error instanceof Error ? error.message : String(error) }); } finally { await this.artifacts.removeWorktree(workspace.rootPath, path).catch((reason: unknown) => console.error('[Integration] Could not clean integration worktree.', reason)); }
  }
  private gate(run: ReturnType<RunRepository['findRequired']>): void { if (run.status !== 'completed' || run.candidatePublishState !== 'published' || !run.candidateCommitSha || run.validationStatus !== 'passed') throw new Error('Review requires a completed Run with a published candidate SHA and passed validation.'); }
  private async commit(path: string, ref: string): Promise<string> { const value = await this.git(path, ['rev-parse', '--verify', `${ref}^{commit}`]); if (value.exitCode !== 0) throw new Error(`Commit ${ref} is unavailable.`); return value.stdout.trim(); }
  private async remote(path: string, remote: string, branch: string): Promise<string | null> { const value = await this.git(path, ['ls-remote', remote, `refs/heads/${branch}`]); return value.exitCode === 0 ? value.stdout.trim().split(/\s+/)[0] || null : null; }
  private async state(path: string): Promise<{ head: string; status: string }> { const [head, status] = await Promise.all([this.git(path, ['rev-parse', 'HEAD']), this.git(path, ['status', '--porcelain=v1', '--untracked-files=all'])]); return { head: head.stdout.trim(), status: status.stdout }; }
  private async must(path: string, args: string[], message: string): Promise<void> { const value = await this.git(path, args); if (value.exitCode !== 0) throw new Error(`${message} ${detail(value)}`); }
}

export class SupervisedIntegrationValidator implements IntegrationValidator {
  public constructor(private readonly supervisor: ProcessSupervisor = new WindowsProcessSupervisor(), private readonly deadlineMs = 30 * 60_000) {}
  public async validate(path: string): Promise<{ passed: boolean; evidence: string }> { try { const json = JSON.parse(await readFile(join(path, 'package.json'), 'utf8')) as { scripts?: Record<string, string> }; const scripts = ['typecheck', 'lint', 'test', 'build'].filter((item) => typeof json.scripts?.[item] === 'string'); if (!scripts.length) return { passed: false, evidence: 'No deterministic validation scripts are configured.' }; const deadline = Date.now() + this.deadlineMs; const lines: string[] = []; for (const script of scripts) { const value = await this.run(path, script, deadline); lines.push(`$ npm run ${script}\n${value.output}`); if (value.exitCode !== 0 || value.timeout) return { passed: false, evidence: lines.join('\n') }; } return { passed: true, evidence: lines.join('\n') }; } catch { return { passed: false, evidence: 'No package.json is available.' }; } }
  private async run(path: string, script: string, deadline: number): Promise<{ exitCode: number | null; output: string; timeout: boolean }> { const id = `integration-validation-${randomUUID()}`; await this.supervisor.start({ executionId: id, executablePath: process.platform === 'win32' ? 'cmd.exe' : 'npm', arguments: process.platform === 'win32' ? ['/d', '/s', '/c', `npm run ${script}`] : ['run', script], workingDirectory: path, environment: Object.fromEntries(Object.entries(process.env).filter((entry): entry is [string, string] => entry[1] !== undefined)) }); let timer: ReturnType<typeof setTimeout> | undefined; try { const complete = this.supervisor.waitForCompletion(id); const remaining = Math.max(0, deadline - Date.now()); const value = await Promise.race([complete, new Promise<'timeout'>((resolve) => { timer = setTimeout(() => resolve('timeout'), remaining); })]); if (value === 'timeout') { await this.supervisor.cancelOwnedTree(id); const stopped = await complete; return { exitCode: stopped.exitCode, output: bounded(stopped.stdout + stopped.stderr), timeout: true }; } return { exitCode: value.exitCode, output: bounded(value.stdout + value.stderr), timeout: false }; } finally { if (timer) clearTimeout(timer); } }
}
const bounded = (value: string): string => Buffer.byteLength(value) > 64 * 1024 ? Buffer.from(value).subarray(0, 64 * 1024).toString('utf8') : value;
const REVIEW_PATCH_BYTES = 512 * 1024;
const boundedGit = (cwd: string, args: string[], limit: number): Promise<{ stdout: string; stderr: string; exitCode: number; truncated: boolean }> => new Promise((resolve, reject) => { const child = spawn('git', ['-C', cwd, ...args], { shell: false, windowsHide: true, stdio: ['ignore', 'pipe', 'pipe'] }); let stdout = ''; let stderr = ''; let bytes = 0; let truncated = false; child.stdout.setEncoding('utf8'); child.stderr.setEncoding('utf8'); child.stdout.on('data', (chunk: string) => { const chunkBytes = Buffer.byteLength(chunk); if (bytes < limit) { const remaining = limit - bytes; stdout += chunkBytes <= remaining ? chunk : Buffer.from(chunk).subarray(0, remaining).toString('utf8'); } bytes += chunkBytes; truncated ||= bytes > limit; }); child.stderr.on('data', (chunk: string) => { stderr += chunk; }); child.once('error', reject); child.once('close', (exitCode) => resolve({ stdout, stderr, exitCode: exitCode ?? -1, truncated })); });
const parse = (raw: string): { verdict: ReviewerVerdict; summary: string; findings: string } | undefined => { try { const value = JSON.parse(raw) as Record<string, unknown>; return (value.verdict === 'PASS' || value.verdict === 'FAIL' || value.verdict === 'NEEDS_ATTENTION') && typeof value.summary === 'string' && typeof value.findings === 'string' ? { verdict: value.verdict, summary: value.summary, findings: value.findings } : undefined; } catch { return undefined; } };
const prompt = (candidate: string, dev: string, evidence: unknown): string => `Read-only review. Do not edit files or run Git; deterministic evidence follows. Candidate ${candidate}; reviewed dev ${dev}. Reply with exactly one JSON object: {"verdict":"PASS|FAIL|NEEDS_ATTENTION","summary":"...","findings":"..."}.\nEVIDENCE:\n${JSON.stringify(evidence)}`;
const detail = (value: { stdout: string; stderr: string }): string => value.stderr.trim() || value.stdout.trim() || 'Git returned an error.';
const isMissing = (error: unknown): boolean => (error as NodeJS.ErrnoException).code === 'ENOENT';

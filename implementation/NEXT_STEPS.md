# Next Steps — Execution Order

## NOW — Step 1: accept V2 authority

Review:
- Master Authority;
- Product;
- UX;
- Runtime;
- FCC;
- Planner/Runs;
- Workers.

If no conceptual contradiction remains, stop editing product architecture casually during implementation.

New discoveries may amend authority through explicit decision records.

## Step 2: repair/normalize FCC installation

Current goal:
- global `fcc-*` launchers available;
- server not merely runnable from source checkout;
- known version;
- reliable PATH.

Use:
`FCC_CLI_VALIDATION_CHECKLIST.md`.

## Step 3: validate Codex + Claude first

Even though V1 should detect all FCC agents, start validation with:
- Codex;
- Claude Code.

Test both with:
- same Lightning model;
- same Super model;
- same clean repository task.

Then probe other installed FCC agents.

## Step 4: write capability matrix

Do not start NightShift adapters before knowing:
- exact launch command;
- exact model selection mechanism;
- headless behavior;
- output behavior;
- cancellation.

## Step 5: choose first adapter

Pick winner by capability/benchmark.

This is not a permanent exclusive choice.

## Step 6: create dedicated NightShift repository

Use this V2 pack as project authority.

Archive old Qwen Master Pack outside active docs.

## Step 7: Codex builds thin shell

Provide:
- V2 authority pack;
- Illustrator assets;
- current mockups.

Scope:
- Electron/React/TS/Vite;
- SQLite;
- workspace tabs;
- nav;
- current Planner;
- current Worker shell;
- right explorer;
- quick Terminal/Explorer/IDE.

Do not build all future features.

## Step 8: immediate vertical slice

Implement:
- FccGateway;
- AgentRegistry;
- first AgentAdapter;
- WorktreeService;
- Planner scheduler;
- Run persistence/events;
- streaming Run UI.

Prove:
`Task → Run → FCC coding agent → repository change → completion`.

## Step 9: second adapter and real benchmark

Only after this point expand Workers/hub broadly.

## Step 10: dogfood

Queue NightShift development work inside NightShift.
